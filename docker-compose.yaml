services:
  frontend:
    image: final-project-test
    container_name: final-project-app
    restart: always
    ports:
      - "3000:3000"
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
